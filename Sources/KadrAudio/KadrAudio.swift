/// KadrAudio — music-library integration for [Kadr](https://github.com/SteliyanH/kadr).
///
/// Resolves `MPMediaItem` values into kadr's `AudioTrack`, and is honest about the
/// large fraction of a typical music library that cannot be used at all.
public enum KadrAudio {
    /// The package's semantic version.
    public static let version = "0.2.0"
}
